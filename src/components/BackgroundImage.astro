---
interface Props {
  src: string;
  alt: string;
}

const { src, alt } = Astro.props;
---

<div class="fixed -z-10 h-screen w-screen overflow-hidden">
  <img
    alt={alt}
    src={src}
    class="h-full w-full object-cover"
  />
</div>
